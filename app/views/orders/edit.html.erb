<div class="container">
  <div class="row">
    <div class="col-sm-12">

        <div class="cart-container">
            <div class="big-shop-card-header">
                <div class="shop-card-logo-avatar" id="cart-avatar">
                </div>
                <div class="cart-shop-title">
                  <h4><%= @order.cart.shop.name%></h4>
                </div>
            </div>

            <div class="big-shop-card" id="big-shop-cart">
            </div>

            <div class="order-title">
              <h3 style="margin-top: 10px;"> ORDER PICKING-UP</h3>
            </div>

            <div class="cart-info-container">
              <div class="cart-info">
                <p>Pick-up time</p>
                <p>Today : 7.30Pm - 8.30pm</p>
              </div>
              <div class="cart-info">
                <p>Directions</p>
                <%= @order.cart.shop.location%></div>
              <div class="cart-info" id="total-order">
                <p></p>Total</p>
                <p style="margin-right: 50px;"><%= @order.cart.shop.price%> SGD</p>
              </div>
              <p style="font-size: 14px; margin-left: 10px;">I confirm I let the food merchant validate my order at the <%= @order.cart.shop.name%>'s cachier and I have been given the order.</p>
            </div>


            <div class="col-sm-12">
                  <%= simple_form_for([@shop, @cart, @order]) do |f|%>
                    <%= f.input :code_validated, as: :boolean, checked_value: true, unchecked_value: false %>
                    <%= f.submit "CONFIRM", class:"btn btn-action btn-duck-blue btn-medium border-rad" %>
                  <% end %>
            </div>

    </div>
  </div>
</div>
